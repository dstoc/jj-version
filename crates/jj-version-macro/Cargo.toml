[package]
name = "jj-version-macro"
version = "0.1.2"
edition = "2024"
description = "Proc macro for jj-version"
license = "MIT"
readme = "../../README.md"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
syn = { version = "2", features = ["full"] }
